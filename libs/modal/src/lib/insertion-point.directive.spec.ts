import { InsertionPointDirective } from './insertion-point.directive';

describe('InsertionPointDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertionPointDirective();
    expect(directive).toBeTruthy();
  });
});
