export namespace ModalActions {
  export class Show {
    static readonly type = '[ModalActions] Show modal';
  }

  export class Close {
    static readonly type = '[ModalActions] Close modal';
  }
}
