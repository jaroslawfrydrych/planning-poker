import { TextFixedWidthDirective } from './text-fixed-width.directive';

describe('TextFixedWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFixedWidthDirective();
    expect(directive).toBeTruthy();
  });
});
